---
import CopyButton from './CopyButton.svelte';

interface Props {
	title?: string;
}

const { title = '範例程式' } = Astro.props;
---

<div class="code-block">
	<div class="code-title">
		<span>{title}</span>
		<CopyButton client:visible />
	</div>
	<slot />
</div>
