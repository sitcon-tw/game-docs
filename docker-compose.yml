name: sitconcamp2026-game-docs

services:
  docs:
    build:
      context: .
    image: sitconcamp2026-game-docs:latest
    ports:
      - "${DOCS_PORT:-4321}:4321"
    environment:
      HOST: 0.0.0.0
      PORT: "4321"
      ASTRO_TELEMETRY_DISABLED: "1"
    restart: unless-stopped
